import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: {
      main: "#D71600",
    },
    text: {
      primary: "#000000",
      secondary: "#000000",
    },
    background: {
      default: "#FFFFFF",
    },
  },
});
export default theme;
