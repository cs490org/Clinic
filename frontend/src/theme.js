import { createTheme } from "@mui/material";
const theme = createTheme({
  palette: {
    primary: {
      main: "#D71600",
    },
    text: {
      primary: "#000000",
      secondary: "#FFFFFF",
    },
    background: {
      default: "#FFFFFF",
    },
  },
});
export default theme;
