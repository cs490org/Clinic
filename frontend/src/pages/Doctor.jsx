import { Box, Typography } from "@mui/material";

function Doctor() {
  return (
    <Box sx={{ mt: "6rem" }}>
      <Typography variant={"h1"} textAlign={"center"}>
        Doctor page
      </Typography>
    </Box>
  );
}

export default Doctor;
